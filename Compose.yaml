services:

  web:
    build: .
    ports:
      - "8000:8000"

    volumes:
      - .:/app

    env_file:
      - .env

    depends_on:
      - db

  db:
    image: postgres:16-alpine

    environment:
      POSTGRES_DB: isatisstack
      POSTGRES_USER: lartenbashi
      POSTGRES_PASSWORD: '@Mirhosein1379??'

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: